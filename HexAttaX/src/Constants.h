#pragma once

namespace Constants
{
	// window
	const int WINDOW_WIDTH = 1280;
	const int WINDOW_HEIGHT = 720;

	// hex
	const float HEX_RAD = WINDOW_WIDTH / 20.f;

	// text
	const int FONT_SIZE = WINDOW_HEIGHT / 5.f;
	const float FONT_OFFSET_Y = FONT_SIZE / -1.6f;
	const int FONT_SIZE_TILE = WINDOW_HEIGHT / 7.f;
	const float FONT_OFFSET_Y_TILE = FONT_SIZE_TILE / -1.6f;
}