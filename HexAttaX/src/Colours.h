#pragma once

#include <SFML/Graphics.hpp>


namespace Colours
{
	const sf::Color screenBackground(220, 220, 220);

	const sf::Color tileBackground(230, 230, 230);
	const sf::Color tileHover(220, 220, 220);
	const sf::Color tileOutline(160, 160, 160);
}